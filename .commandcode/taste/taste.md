- Wants the assistant to **scan recent/interrupted work and report what's pending (logic, features, in-progress items) before proposing or executing a fix**. Skipping straight to "continue from the handoff doc" or assuming scope is wrong — must first inventory git log / work-orders / stashes / worktrees and present what was actually in progress. Confidence: 0.9
- Communicates in **Korean** and prefers the assistant to respond in Korean. Confidence: 0.95
- Prefers a **multi-project setup under a shared parent** (e.g. `Project/flucto` and `Project/flucto_landing` as siblings) rather than a single monorepo. When a handoff mentions "the project", check the parent directory for sibling repos that may also have interrupted work. Confidence: 0.7
- Uses **handoff artifacts named `CONTINUATION.md` / work-order docs (`docs/work-orders-*.md`)** to record interrupted work; trust these as the primary source of state, but always cross-check against actual `git log`, `git status`, `git stash list`, and `git worktree list` before acting. Confidence: 0.85
- When multiple candidate interrupted items exist across repos, prefers being **asked to disambiguate** (multi-select) rather than the assistant defaulting to one. Confidence: 0.8
- For nginx-served static sites, after `scp` (replacing the dist) a `sudo nginx -s reload` is required — workers cache open file descriptors and keep serving the prior `index.html` even after the file is gone. The reload (not the scp) is what makes the new dist actually visible. Confidence: 0.9
- In CI deploy steps, all `sudo` calls should use `sudo -n` (non-interactive) and `ssh` should use `ssh -n`, with any interactive remote command getting `< /dev/null` redirected — otherwise a missing sudoers rule or stdin grab hangs the job past 2 minutes instead of failing fast. Confidence: 0.9
- When chaining fallbacks (`|| true`, `|| cmd2 || cmd3`), prefer an explicit if/elif that echoes which path actually succeeded — silent fallbacks make the next failure impossible to diagnose from CI logs. Confidence: 0.8
- For client-only UI affordances hidden behind a query string flag (e.g. `?i18n=1`), use a lazy `useState(() => check window.location.search)` rather than `useEffect` + `setState` — avoids a lint warning about setState in effect AND avoids a hydration mismatch when the prerendered HTML (no query) and the first client render (no query) both agree on `false`. Confidence: 0.85
- When the user asks "왜 이렇게 되는거지?" / "이거 왜 이래?" — they want a **causal explanation of the observed behavior, not a fix**. Investigate, explain the mechanism (prerender → hydrate, framer-motion initial, etc.), and leave the code untouched. Only propose changes if the user follows up with "고쳐줘" or similar. If the explanation surfaces multiple reasonable fix paths, offer them but do not act on one by default. Confidence: 0.85
- When CI is green but the live site still serves old content, **diagnose layer-by-layer with direct evidence before changing anything more**: check the run's `conclusion` (not just `status: completed`), diff local vs live asset hashes / build-stamps, SSH in read-only (via the `~/.ssh/config` host alias) to inspect the files actually on disk, then check DNS for a CDN proxy and bypass it with `curl --resolve host:443:<origin-ip>` plus a cache-buster query (`?nocache=$(date +%s)`) to isolate which layer is stale. Stop stacking speculative origin fixes (touch/reload) once the origin disk is proven correct. Confidence: 0.9
- On this stack the true root cause of "deployed but stale page" was the **Cloudflare edge cache** — the zone has a cache-everything rule that ignores the origin's `Cache-Control: no-cache` (`cf-cache-status: HIT` with `age: 840` on a plain GET, while a buster query returned `MISS` + fresh bytes). Fix = post-deploy Cloudflare API purge (`CF_ZONE_ID` + `CF_API_TOKEN` secrets, token needs Zone.Cache Purge); until those are configured, verify deploys with cache-buster curls, never plain GETs. Confidence: 0.85
- In GitHub Actions, a step depending on optional secrets must **runtime-check them inside `run:`** and `exit 0` with an explanatory line when unset — a step-level `env:` block is NOT visible to that step's own `if:` expression, so `if: env.X != ''` silently skips the step even when the secret exists. Optional steps should skip gracefully with a logged reason, never fail the job. Confidence: 0.8
- When a working diagnosis is later disproven (e.g. "nginx fd-cache" → actually Cloudflare edge cache), **rewrite the affected gotchas in CONTINUATION.md / README to the corrected cause** so the next session doesn't inherit the wrong mental model. Confidence: 0.75
- Session wrap-up hygiene after a refactor/migration: `grep` for exports orphaned by the refactor and delete them, sync README / handoff docs to actual behavior, then lint + build + push + live-verify before reporting done. Confidence: 0.7
